SOLUTION
  Integer silnia(Integer a[], Integer N)
  DO
    IF N == 0
    THEN
      a[0] = 1;
    ELSE
      silnia(a, N - 1);
      a[N] = a[N - 1] * N;
    FI
  RETURNED

  Integer N = 5;
  Integer aint[N];

  silnia(aint, N - 1);

  FOR i IN N
  DO
    PRINT i;
    PRINT aint[i];
  DONE
CYA
