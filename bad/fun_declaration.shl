/* function declaration allowed only in outermost block */

SOLUTION
  Boolean declare = True;
  IF declare
  THEN
    Integer sum(Integer a, Integer b, REF Integer c)
    DO
      c = a + b ;
      RETURN c;
    RETURNED
  FI
CYA
